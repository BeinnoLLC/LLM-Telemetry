# P4-11 · Projects entry in the nav drawer and a homepage card

**Complexity:** 🟢 low  **Estimate:** ⏱ 1h  **Depends:** #3, #4
**Status:** not started

## Problem

The Projects view needs a way in. The current navigation is a six-chip row
(`Live Flow Usage Cost Health Detail`) that #4 replaces with a left drawer.

## Change

- Add `Projects` to the drawer nav (#4) with an icon, and to the icon-rail mode.
- Add a homepage card (#3) summarising the top project by spend and the
  unattributed share, linking into the view.
- Register the view in the hash router (#5) as `#/projects`.

**Do not** add a seventh chip to the `#tabs` row — #4 deletes it, and the chip
row already overflows on mobile.

## Acceptance

- [ ] Drawer entry present in expanded, rail and off-canvas modes.
- [ ] Homepage card shows top project + unattributed share, both from the
      payload rather than hardcoded.
- [ ] `#/projects` deep-links correctly and back/forward work.
- [ ] Active-state styling matches the other drawer entries.

## Notes

Blocked on the nav shell. Building it against the chip row means writing code
twice and resolving a conflict in between.
